# Count NA values by group

Returns patterns of missingness across one or more variables, with
number of cases for each pattern.

## Usage

``` r
count_na(
  .data,
  ...,
  .label_missing = NA_character_,
  .label_valid = "OK",
  .add_percent = FALSE
)
```
