# Remove missing values

Returns a vector with `NA`s removed. Similar to
[`stats::na.omit.default()`](https://rdrr.io/r/stats/na.fail.html), but
does not add attributes to returned value.

## Usage

``` r
discard_na(x)
```
