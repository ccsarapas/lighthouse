# Test whether a data frame contains SPSS variable or value labels

Checks if a data frame contains any SPSS / haven variable labels, value
labels, or format attributes.

## Usage

``` r
is_spss(.data)
```

## Arguments

- .data:

  A data frame.

## Value

`TRUE` if the data frame contains any SPSS labels, `FALSE` otherwise.
