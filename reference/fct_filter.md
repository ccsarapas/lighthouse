# Filter by and drop factor levels simultaneously

Filters dataframe by specified levels of `.fct`, then drops filtered
levels from `.fct`.

## Usage

``` r
fct_filter(.data, .fct, .keep = NULL, .drop = NULL)
```
