# Summarize a dataframe's column types - DEPRECATED

Deprecated in favor of
[`cols_info()`](https://ccsarapas.github.io/lighthouse/reference/cols_info.md),
which provides more information and features and is more stable.

Returns class and type for each column in `.data`.

## Usage

``` r
get_col_types(.data)
```
