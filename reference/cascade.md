# Utilities for service cascades

Functions for working with service cascades.

## Usage

``` r
cascade_fill_bwd(data, vars)

cascade_fill_fwd(data, vars, fill = c("NA", "FALSE"))

cascade_summarize(data, vars)
```
